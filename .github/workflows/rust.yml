name: Rust SDK

on:
  push:
    branches: [master, main]
    paths:
      - 'rust/**'
      - 'meta/**'
      - 'spec/**'
      - 'test-assets/**'
      - '.github/workflows/rust.yml'
    tags:
      - 'v[0-9]+.[0-9]+.[0-9]+'
  pull_request:
    branches: [master, main]
    paths:
      - 'rust/**'
      - 'meta/**'
      - 'spec/**'
      - 'test-assets/**'
      - '.github/workflows/rust.yml'

env:
  CARGO_TERM_COLOR: always

jobs:
  test:
    name: Test Rust ${{ matrix.rust }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
        rust: [stable, beta]
        include:
          - os: ubuntu-latest
            rust: nightly

    steps:
      - uses: actions/checkout@v6
        with:
          submodules: recursive

      - name: Install Rust ${{ matrix.rust }}
        uses: dtolnay/rust-toolchain@master
        with:
          toolchain: ${{ matrix.rust }}
          components: rustfmt, clippy

      - name: Cache cargo registry
        uses: actions/cache@v5
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            rust/target
          key: ${{ runner.os }}-cargo-${{ matrix.rust }}-${{ hashFiles('rust/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-cargo-${{ matrix.rust }}-

      - name: Install protoc
        uses: arduino/setup-protoc@v3
        with:
          version: '29.3'
          repo-token: ${{ secrets.GITHUB_TOKEN }}

      - name: Build
        working-directory: rust
        run: cargo build --verbose --all-features --all-targets

      # `--all-features` is not optional here. The Avro runtime lives behind
      # `avro` and the `jstruct` binary behind `cli`; without them the Avro
      # round-trip tests and the whole CLI suite compile to zero tests and
      # report success.
      - name: Run tests
        working-directory: rust
        env:
          JSTRUCT_REQUIRE_PROTOC: '1'
        run: cargo test --verbose --all-features

      - name: Run clippy
        if: matrix.rust == 'stable' && matrix.os == 'ubuntu-latest'
        working-directory: rust
        run: cargo clippy --all-features --all-targets -- -D warnings

      - name: Check formatting
        if: matrix.rust == 'stable' && matrix.os == 'ubuntu-latest'
        working-directory: rust
        continue-on-error: true
        run: cargo fmt -- --check

  build:
    name: Build package
    runs-on: ubuntu-latest
    needs: test

    steps:
      - uses: actions/checkout@v6
        with:
          submodules: recursive

      - name: Install Rust stable
        uses: dtolnay/rust-toolchain@stable

      - name: Build release
        working-directory: rust
        run: cargo build --release

      - name: Package
        working-directory: rust
        run: cargo package --allow-dirty

      - name: Upload build artifacts
        uses: actions/upload-artifact@v6
        with:
          name: rust-package
          path: rust/target/package/*.crate

  publish:
    name: Publish to crates.io
    runs-on: ubuntu-latest
    needs: build
    if: startsWith(github.ref, 'refs/tags/v')

    steps:
      - uses: actions/checkout@v6
        with:
          submodules: recursive

      - name: Install Rust stable
        uses: dtolnay/rust-toolchain@stable

      - name: Extract version from tag
        id: get_version
        run: |
          VERSION=${GITHUB_REF#refs/tags/v}
          echo "VERSION=$VERSION" >> $GITHUB_OUTPUT
          echo "Extracted version: $VERSION"

      - name: Update Cargo.toml version
        working-directory: rust
        run: |
          sed -i 's/^version = ".*"/version = "${{ steps.get_version.outputs.VERSION }}"/' Cargo.toml
          echo "Updated Cargo.toml to version ${{ steps.get_version.outputs.VERSION }}"
          grep '^version' Cargo.toml

      - name: Publish to crates.io
        working-directory: rust
        env:
          CARGO_REGISTRY_TOKEN: ${{ secrets.CRATES_IO_TOKEN }}
        run: cargo publish
