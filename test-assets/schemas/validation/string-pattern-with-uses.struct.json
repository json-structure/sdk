{
  "$schema": "https://json-structure.org/meta/extended/v0/#",
  "$uses": ["JSONStructureValidation"],
  "type": "string",
  "pattern": "^[A-Z][a-z]+$",
  "$comment": "Uses 'pattern' keyword WITH validation extension enabled via $uses - should be enforced"
}
