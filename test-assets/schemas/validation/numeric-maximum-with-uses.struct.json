{
  "$schema": "https://json-structure.org/meta/extended/v0/#",
  "$uses": ["JSONStructureValidation"],
  "type": "int32",
  "maximum": 100,
  "$comment": "Uses 'maximum' keyword WITH validation extension enabled via $uses - should be enforced"
}
