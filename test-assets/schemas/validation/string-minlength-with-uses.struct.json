{
  "$schema": "https://json-structure.org/meta/extended/v0/#",
  "$uses": ["JSONStructureValidation"],
  "type": "string",
  "minLength": 3,
  "$comment": "Uses 'minLength' keyword WITH validation extension enabled via $uses - should be enforced"
}
