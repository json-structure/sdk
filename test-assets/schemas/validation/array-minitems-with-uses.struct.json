{
  "$schema": "https://json-structure.org/meta/extended/v0/#",
  "$uses": ["JSONStructureValidation"],
  "type": "array",
  "items": { "type": "string" },
  "minItems": 2,
  "$comment": "Uses 'minItems' keyword WITH validation extension enabled via $uses - should be enforced"
}
