{
  "$schema": "https://json-structure.org/meta/core/v0/#",
  "$id": "https://example.com/avro/altnames",
  "$uses": ["JSONStructureAlternateNames"],
  "name": "Record",
  "type": "object",
  "altnames": { "proto": "LegacyRecord" },
  "properties": {
    "first-name": { "type": "string", "altnames": { "proto": "firstName" } },
    "status": {
      "type": "string",
      "enum": ["in-stock", "out-of-stock"],
      "altenums": { "proto": { "in-stock": "IN_STOCK", "out-of-stock": "OUT_OF_STOCK" } }
    }
  },
  "required": ["first-name", "status"]
}