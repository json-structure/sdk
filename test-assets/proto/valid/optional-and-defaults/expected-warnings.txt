#/properties/retries: proto3 cannot express `default`; the value is not enforced
#/properties/enabled: proto3 cannot express `default`; the value is not enforced
#/properties/kind: proto3 cannot express `const`; the value is not enforced
