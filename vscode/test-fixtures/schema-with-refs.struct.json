{
    "$schema": "https://json-structure.org/meta/core/v0/#",
    "$id": "https://example.com/schema-with-refs",
    "name": "SchemaWithRefs",
    "type": "object",
    "properties": {
        "primaryAddress": { "type": { "$ref": "#/definitions/Address" } },
        "billingAddress": { "type": { "$ref": "#/definitions/Address" } },
        "person": { "type": { "$ref": "#/definitions/Person" } }
    },
    "$root": "#/definitions/Person",
    "definitions": {
        "Address": {
            "type": "object",
            "properties": {
                "street": { "type": "string" },
                "city": { "type": "string" },
                "zipCode": { "type": "string" }
            },
            "required": ["street", "city"]
        },
        "Person": {
            "type": "object",
            "properties": {
                "name": { "type": "string" },
                "age": { "type": "int32" },
                "address": { "type": { "$ref": "#/definitions/Address" } }
            },
            "required": ["name"]
        }
    }
}
